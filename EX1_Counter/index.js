let count = 0;

function subtraction() {
    const num = document.getElementById('number');

    count--;
    num.innerText = count;
}
function summation() {
    const num = document.getElementById('number');

    count++;
    num.innerText = count;
}
